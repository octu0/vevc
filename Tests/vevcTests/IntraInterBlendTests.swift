import XCTest
@testable import vevc

final class IntraInterBlendTests: XCTestCase {
    func testDeprecated() {
        // Intra/inter boundary blend was deprecated and removed as dead code.
    }
}

